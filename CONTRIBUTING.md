Make sure to leave alone the files in the `src/` folder as they are auto-generated.

To update them after changing files in the `ts/` folder, run `npm run compile` with typescript installed (`npm i -g typescript`).